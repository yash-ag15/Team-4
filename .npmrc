# Committed so all 8 teammates get identical installs.

# Without this file every teammate hits the same ERESOLVE wall and "fixes" it a different
# way, which is how lockfiles diverge.
legacy-peer-deps=true
