/**
 * projects-list.tsx — starter template placeholder.
 *
 * The real Katalyst dashboard lives at src/app/(student)/dashboard/page.tsx.
 * This component exists only to satisfy the import in src/app/dashboard/page.tsx
 * so the build does not crash. It renders nothing.
 */
export function ProjectsList() {
  return null
}
